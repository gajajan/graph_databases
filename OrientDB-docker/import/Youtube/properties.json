{
      "source": { "file": { "path": "/csv/Youtube/properties.csv" } },
      "extractor": { "csv": {
        "columns": ["userId:integer", "firstName:string", "age:integer"]
      } },
      "transformers": [
        {
            "command": {
              "command": "UPDATE User SET firstName = '${input.firstName}', age = ${input.age} WHERE userId = ${input.userId}"
            }
          }
      ],
      "loader": {
        "orientdb": {
          "dbURL": "plocal:../databases/Youtube",
          "dbType": "graph",
          "dbUser": "root",
          "dbPassword": "root",
          "useLightweightEdges": false,
          "tx": false,
          "wal": false,
          "batchCommit": 1000
        }
      }
    }