{
  "configuration": {"psrsllel": true},
    "source": { "file": { "path": "/csv/Youtube/com-youtube.ungraph.tsv" } },
    "extractor": { 
      "csv": {
        "columns": ["FromNodeId:integer","ToNodeId:integer"],
        "separator": "\t"
      } 
    },
    "transformers": [
      { "merge": { "joinFieldName": "FromNodeId", "lookup": "User.userId" } },
      { "edge": { "class": "FRIENDS",
                  "joinFieldName": "ToNodeId",
                  "lookup": "User.userId",
                  "direction": "out" 
                }
      },
      { "field": { "fieldNames": ["FromNodeId", "ToNodeId"], "operation": "remove" } }
    ],
    "loader": {
      "orientdb": {
         "dbURL": "plocal:../databases/Youtube",
         "dbUser": "root",
         "dbPassword": "root",
         "dbType": "graph",
         "tx": false,
         "wal": false,
         "batchCommit": 1000,
         "useLightweightEdges":false
      }
    }
}